{
  "name": "sketch-export-presets",
  "description": "Import and Save Sketch Export Preferences",
  "version": "1.0.0",
  "engines": {
    "sketch": ">=49.0"
  },
  "skpm": {
    "name": "sketch-export-presets",
    "manifest": "src/manifest.json",
    "main": "sketch-export-presets.sketchplugin",
    "assets": [
      "assets/**/*"
    ],
    "sketch-assets-file": "sketch-assets/icons.sketch"
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.7.0"
  },
  "author": "Kevin Gutowski",
  "repository": {
    "type": "git",
    "url": "https://github.com/KevinGutowski/Sketch-Export-Presets"
  },
  "dependencies": {
    "@skpm/dialog": "^0.4.0",
    "@skpm/fs": "^0.2.5"
  }
}
